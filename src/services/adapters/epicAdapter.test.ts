import { describe, test } from 'vitest';

describe('Unit: epicAdapter', () => {
  describe('estimate', () => {
    test.todo('Если estimate = 0, то estimate должен стать 0h');
    test.todo('Если estimate = 4, то estimate должен стать 4h');
    test.todo('Если estimate = 10, то estimate должен стать 1d 2h');
    test.todo('Если estimate = 44, то estimate должен стать 1w 4h');
    test.todo('Если estimate = 56, то estimate должен стать 1w 2d');
    test.todo('Если estimate = 58, то estimate должен стать 1w 2d 2h');
  });
});
