import { describe, test } from 'vitest';

describe('Unit: OwnerLink', () => {
  test.todo('Если передан link, должен отрендерить ссылку');
  test.todo('Если не передан link, ссылки не должно быть');
});
