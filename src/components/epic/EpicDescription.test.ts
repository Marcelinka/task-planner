import { describe, test } from 'vitest';

describe('Unit: EpicDescription', () => {
  test.todo('Не должен показывать owner, если не передан');
  test.todo('Должен показывать owner, если он передан');

  test.todo('Не должен показывать projects, если они не переданы');
  test.todo('Должен показать 1 project, если передан массив из 1 элемента');
  test.todo('Должен показать 2 projects, если передан массив из 2 элементов');
});
